name: GitHub actions - To Test Repo - Trigger Jenkins Job
run-name: Test Repo - Trigger Jenkins Job

on:
  push:
    branches: ["master"]

env:
  owner_name: Mani

jobs:
  build_test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Job Info
      run: echo "Job build_test is executing as part of new workflow - from $owner_name"

    - uses: actions/checkout@v4

    - name: Setup Maven Action
      uses: s4u/setup-maven-action@v1.7.0
      with:
        checkout-fetch-depth: 0
        java-version: 21.0.3
        java-distribution: temurin
        maven-version: 3.9.8
    - name: Test with Maven
      run: mvn -B test --file pom.xml
    
    - name: connect to LAb and trigger jenkins job
      uses: cross-the-world/ssh-scp-ssh-pipelines@latest 
      with:
        host: http://54.167.164.84
        user: labuser
        pass: Nuvelabs123$
        port: 
        connect_timeout: 30s 
        first_ssh: |
          curl -X POST "http://54.167.164.84:8080/job/GitHubTrigger/build" --user "admin:11fbc19d0b76d3483a94d294c683f5a111"
